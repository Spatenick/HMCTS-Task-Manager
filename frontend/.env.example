# Base URL of the backend API.
# Leave blank when running `npm run dev` — Vite proxies /api to http://localhost:4000.
# Set this when building for production, e.g. VITE_API_BASE=https://api.example.com/api
VITE_API_BASE=
